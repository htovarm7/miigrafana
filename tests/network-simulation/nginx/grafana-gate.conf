# Simulates the NSG rule: only the VPN subnet reaches Grafana.
server {
    listen 3000;

    location / {
        allow 172.30.2.0/24;
        deny all;

        proxy_pass http://grafana:3000;
        proxy_set_header Host $host;
    }
}
